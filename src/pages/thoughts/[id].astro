---
import { getCollection, render } from "astro:content";

import dayjs from "dayjs";
import advancedFormat from "dayjs/plugin/advancedFormat";
import utc from "dayjs/plugin/utc";

import Layout from "layouts/PostLayout.astro";
import Link from "components/Link.astro";
import { parseDateFromFilePath } from "lib/blog/utils";

dayjs.extend(utc);
dayjs.extend(advancedFormat);

const { entry } = Astro.props;

const { Content } = await render(entry);

export const prerender = true;

export async function getStaticPaths() {
    const blogEntries = await getCollection("blog");
    return blogEntries
        .filter((entry) => {
            if (import.meta.env.DEV) return true;
            return !entry.id.startsWith("_");
        })
        .map((entry) => {
            const date = parseDateFromFilePath(entry.filePath!);

            return {
                params: { id: entry.id },
                props: {
                    entry: {
                        ...entry,
                        data: {
                            ...entry.data,
                            date,
                        },
                    },
                },
            };
        });
}
---

<Layout
    id={entry.id}
    title={entry.data.title}
    description={entry.data.description}
    tags={entry.data.tags}
    date={entry.data.date}
>
    <div class="flex flex-row items-start justify-start w-full">
        <div
            class="flex flex-col items-start justify-start w-full md:w-[1110px] mt-[20px] md:mt-[30px]"
        >
            <div
                class="flex flex-row items-start justify-start w-full mb-[25px] md:mb-[70px]"
            >
                <h1
                    class="text-[64px] md:text-[168px] leading-none w-full text-wrap"
                >
                    {entry.data.title.toUpperCase()}
                </h1>
            </div>
            <div
                class="flex flex-col-reverse md:flex-row items-start justify-start w-full md:ml-[15px] mb-[80px] md:mb-[140px]"
            >
                <div class="flex flex-col items-start justify-start mr-auto">
                    <p class="text-lg md:text-[24px]">author: rayhanadev</p>
                    <p class="text-lg md:text-[24px]">
                        created: {
                            dayjs(entry.data.date)
                                .utc()
                                .format("MMMM Do, YYYY")
                                .toLowerCase()
                        }
                    </p>
                    <p class="text-lg md:text-[24px]">commit: aadcee0d</p>
                </div>
                <div>
                    <p class="text-lg md:text-2xl leading-9 hidden">
                        <Link href="/thoughts">go back</Link>
                    </p>
                </div>
            </div>
            <div
                class="flex flex-col items-start justify-start w-full prose-sm md:prose prose-zinc md:ml-[15px] text-lg md:text-[24px]"
            >
                <Content />
                <div class="pb-16"></div>
            </div>
        </div>
        <div>
            <!-- Distortion Effect -->
        </div>
    </div>
</Layout>
