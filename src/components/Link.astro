---
const { href, seo = false, ...props } = Astro.props;

export interface Props {
    href: string;
    seo?: boolean;
    [key: string]: unknown;
}
---

<a
    href={href}
    {...seo ? { rel: "noopener", target: "_blank" } : {}}
    class:list={[
        "underline",
        "decoration-zinc-400",
        "hover:decoration-foreground",
        "inline",
        props.class,
    ]}><slot /></a
>
